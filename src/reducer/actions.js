import { createAction } from 'redux-act'

export const receiveData = createAction('receiveData')

export const markAsFavorite = createAction('markAsFavorite')

export const setSorting = createAction('setSorting')

export const onSearch = createAction('onSearch')

export const onSearchSubmit = createAction('onSearchSubmit')

